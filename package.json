{
  "name": "mikael-global",
  "version": "2.0.0",
  "description": "MIKAEL GLOBAL — Data Analysis & Web Development",
  "scripts": {
    "start": "npx @11ty/eleventy --serve --port=8080",
    "build": "npx @11ty/eleventy",
    "watch": "npx @11ty/eleventy --watch"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  },
  "dependencies": {
    "minimatch": "^3.1.5"
  }
}
